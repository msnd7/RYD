# نشر المنصة على Vercel وربطها بقاعدة بيانات دائمة

بعد هذه الخطوات تصبح المنصة على رابط دائم، وتُحفظ كل البيانات على الخادم،
فيراها الجميع من أي جهاز، ولا تضيع عند مسح المتصفح أو تغيير الجهاز.

الوقت المتوقع: أقل من عشر دقائق.

---

## ١) ربط المستودع بـ Vercel

1. افتح [vercel.com](https://vercel.com) وسجّل الدخول بحساب GitHub نفسه.
2. **Add New → Project**.
3. اختر مستودع **`msnd7/RYD`** ثم **Import**.
4. Vercel سيقرأ ملف `vercel.json` تلقائيًا، فلا تحتاج تغيير أي إعداد بناء:
   - Framework: **Vite**
   - Build Command: `npm run build`
   - Output Directory: `dist`
5. **لا تضغط Deploy بعد** — أكمل الخطوة ٢ أولًا.

> إن كان العمل ما زال على الفرع `claude/riyadh-quran-management-platform-6tfefw`،
> فادمجه في `main` أولًا، أو اضبط **Settings → Git → Production Branch** على اسم الفرع.

---

## ٢) إنشاء قاعدة البيانات

من داخل المشروع في Vercel:

1. تبويب **Storage → Create Database**.
2. اختر **Postgres** (المزوّد المعروض، عادةً **Neon**) ثم أنشئها في أقرب منطقة
   (Frankfurt أو Paris أفضل للسعودية).
3. اضغط **Connect** لربطها بالمشروع.

سيضيف Vercel متغيّر `DATABASE_URL` تلقائيًا إلى المشروع. **لا تحتاج إنشاء أي جدول** —
المنصة تنشئ جداولها بنفسها عند أول تشغيل.

> تعمل المنصة مع أي مزوّد PostgreSQL آخر (Supabase مثلًا). يكفي وضع رابط الاتصال
> في متغيّر `DATABASE_URL` يدويًا، ويُفضّل استخدام رابط **Pooler / Connection Pooling**.

---

## ٣) مفتاح توقيع الجلسات

من **Settings → Environment Variables** أضف:

| الاسم | القيمة |
|---|---|
| `AUTH_SECRET` | نص عشوائي طويل (٣٢ محرفًا فأكثر) |

لتوليده: افتح [هذه الصفحة](https://generate-secret.vercel.app/32) أو نفّذ في الطرفية:

```bash
openssl rand -hex 32
```

فعّله للبيئات الثلاث: Production و Preview و Development.

> إن نسيته ستعمل المنصة، لكن جلسات الدخول قد تنتهي عند تغيير قاعدة البيانات.
> ولو غيّرته لاحقًا، سيُطلب من الجميع تسجيل الدخول من جديد (بلا فقدان بيانات).

---

## ٤) النشر

اضغط **Deploy**. بعد دقيقة يظهر رابط مثل `https://ryd.vercel.app`.

للتأكد أن قاعدة البيانات متصلة، افتح:

```
https://<اسم-مشروعك>.vercel.app/api/health
```

يجب أن يظهر: `{"ok":true,"storage":"database"}`

- `"storage":"database"` ← ممتاز، الحفظ مركزي ودائم.
- `"storage":"none"` ← `DATABASE_URL` غير مضبوط؛ راجع الخطوة ٢.

---

## ٥) أول دخول

| الحساب | البريد | الرمز المبدئي |
|---|---|---|
| مدير المجمع | `msnd5033@gmail.com` | `1234` |

ستظهر لك شاشة إلزامية لتعيين رمز خاص بك، ثم تبدأ بإضافة مشرفي المساجد.

---

## ٦) نطاق باسمك (اختياري)

من **Settings → Domains** أضف نطاقك (مثل `ryd.sa`) واتبع تعليمات ضبط DNS.
الشهادة الأمنية (HTTPS) تُصدر تلقائيًا.

---

## كيف يعمل الحفظ؟

- كل بيانات المجمع في **وثيقة واحدة** داخل جدول `ryd_state`، ومعها رقم إصدار.
- عند حفظ أي تعديل يتحقق الخادم من رقم الإصدار. لو كان شخص آخر قد حفظ قبلك،
  يُعاد تطبيق تعديلك فوق النسخة الأحدث تلقائيًا — فلا يضيع عمل أحد.
- المتصفح يسحب تحديثات الآخرين كل عشر ثوانٍ وعند العودة إلى التبويب.
- الفواتير والشواهد تُرفع إلى جدول `ryd_files` وتُقدَّم عبر `/api/files/<id>`
  لأصحاب الجلسات فقط (الحد الأقصى ٣ ميجابايت للملف).
- كلمات المرور **لا تُحفظ كنص**، بل بمعامل تجزئة `scrypt` بملح عشوائي،
  ولا يصل معامل التجزئة إلى المتصفح إطلاقًا.

### الصلاحيات المفروضة على الخادم

الواجهة تخفي ما لا يجوز، والخادم يمنعه فعليًا:

| | مدير المجمع | مشرف المسجد | إداري |
|---|---|---|---|
| الإعدادات العامة والنطاق المكاني | ✅ | — | — |
| تغيير الأدوار والتفويض المالي | ✅ | — | — |
| إضافة وتعديل إداريي مسجده | ✅ | ✅ | — |
| إعادة تعيين رموز الدخول | ✅ | — | — |
| توقيع عقده وتعديل جواله | ✅ | ✅ | ✅ |

---

## التشغيل الكامل على جهازك (اختياري)

```bash
# ١) شغّل قاعدة بيانات PostgreSQL محليًا أو استخدم رابط قاعدة سحابية
export DATABASE_URL="postgresql://user:pass@host:5432/ryd"
export AUTH_SECRET="$(openssl rand -hex 32)"

# ٢) ابنِ الواجهة ثم شغّل الخادم الذي يحاكي بيئة Vercel
npm install
npm run build
npm run dev:api        # http://localhost:3000
```

للتطوير على الواجهة فقط (بلا خادم، حفظ محلي في المتصفح): `npm run dev`.

---

## استكشاف الأعطال

| العَرَض | السبب والحل |
|---|---|
| شارة **«حفظ محلي»** في أعلى الصفحة | لا يوجد اتصال بقاعدة البيانات. افتح `/api/health` وراجع `DATABASE_URL`. |
| شارة **«تعذّر الحفظ»** | انقطاع مؤقت؛ المنصة تعيد المحاولة تلقائيًا، واضغط الشارة لإعادة المحاولة فورًا. |
| `no_database` في ردود الـ API | متغيّر `DATABASE_URL` غير موجود في بيئة Production. |
| الدخول لا يقبل الرمز `1234` | الرمز غُيّر من قبل. يعيد المدير تعيينه من **الإعدادات ← الحسابات والدخول**. |
| فقد المدير رمزه | نفّذ على قاعدة البيانات: `UPDATE ryd_state SET doc = jsonb_set(doc, '{people,0,mustChangePassword}', 'true');` ثم تواصل معنا لإعادة التعيين، أو احذف الصف `ryd_state` لإعادة التهيئة من الصفر (تفقد البيانات). |
| بطء أول طلب بعد فترة سكون | طبيعي في الخطط المجانية (بدء بارد للدوال وقاعدة البيانات). |
